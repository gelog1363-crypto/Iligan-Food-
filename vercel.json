{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "buildCommand": "node ./node_modules/vite/bin/vite.js build --outDir build"
}
